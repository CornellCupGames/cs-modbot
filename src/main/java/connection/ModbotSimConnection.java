package connection;
import packet.Packet;

public class ModbotSimConnection implements Connection {

	public Packet transaction(Packet p, ConnectionType c, boolean receive_packet) {
		return null;
	}

}